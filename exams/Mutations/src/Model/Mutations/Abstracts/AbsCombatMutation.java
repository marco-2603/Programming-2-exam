package Model.Mutations.Abstracts;

import Model.Gerald.Geraldo;
import Model.Gerald.Mutations;

public abstract class AbsCombatMutation extends AbstractMutation {

    public AbsCombatMutation(Geraldo g, Mutations mut) {
        super(g,mut);
    }
}
